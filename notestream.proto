syntax = "proto3";

package notestream;

service NoteStream {
    // unary call
    rpc StreamNotes (stream Note) returns (stream Note);
}

message Note {
    string Sender = 1;
    string Message = 2;
}